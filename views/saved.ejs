<div class="container my-5">

  <h2 class="fw-bold mb-4">💾 Saved Trips</h2>

  <% if (trips.length === 0) { %>
    <p class="text-muted">You haven’t saved any trips yet.</p>
  <% } %>

  <div class="row">
    <% trips.forEach(trip => { %>
      <div class="col-md-4 mb-4">
        <div class="card shadow-sm h-100">

          <img src="<%= trip.image %>"
               class="img-fluid"
               style="height:200px; object-fit:cover;" />

          <div class="card-body">
            <h5><%= trip.name %></h5>
            <p class="text-muted mb-1"><%= trip.location %></p>

            <p>⭐ <%= trip.rating %> (<%= trip.reviewsCount %>)</p>

            <a href="/trips/<%=trip.id%>" class="btn btn-sm btn-outline-primary">
              View Details
            </a>
          </div>
        </div>
      </div>
    <% }) %>
  </div>

</div>
